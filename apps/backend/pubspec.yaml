name: backend
description: Full-stack Dart Backend Server (Cloud Run ready)
version: 1.0.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  logging: ^1.2.0
  http: ^1.1.0
  googleapis_auth: ^1.4.1
  # internal dependencies
  domain_models:
    path: ../../shared/domain_models
  common_logic:
    path: ../../shared/common_logic
  # common_backend:
  #   path: ../../shared/common_backend

dev_dependencies:
  http: ^1.2.0
  test: ^1.25.0
  lints: ^3.0.0
