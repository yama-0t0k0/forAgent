name: reference_code
description: Reference code for development.
publish_to: 'none'
environment:
  sdk: ">=3.6.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.6.0
  common_logic:
    path: ../../shared/common_logic
