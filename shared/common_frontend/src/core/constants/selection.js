/**
 * Constants related to Selection Flow and Status.
 */

/**
 * Selection Flow Lanes
 * Used in SelectionFlowEditor to distinguish between main flow and sub-flow/options.
 */
export const SELECTION_LANE = {
  PRIMARY: 1,
  SECONDARY: 2
};

/**
 * Selection Phase Status
 */
export const SELECTION_STATUS = {
  PENDING: 'pending',
  COMPLETED: 'completed'
};
