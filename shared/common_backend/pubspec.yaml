name: common_backend
description: Shared backend logic including matching and scoring engines.
version: 1.0.0

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^5.6.0
  common_logic:
    path: ../common_logic

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0
